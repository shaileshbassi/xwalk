name: Starter Workflow Demo
env:
  BODY: ${{ github.event.pull_request.body }}

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: demo workflow job
        run: |
          echo "This is the updated demo start workflow"
          echo "$pr_body"
          echo "This is the end for the demo start workflow"